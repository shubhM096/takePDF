{
  "name": "takepdf",
  "version": "1.0.0",
  "private": true,
  "description": "Full-page capture Chrome extension with hyperlink preservation",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "dependencies": {
    "pdfjs-dist": "^6.3.289"
  }
}
