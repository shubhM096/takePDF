describe('Integration', () => {
  test('full PDF capture flow: message → prepare → CDP → download', async () => {
    expect(true).toBe(true);
  });
  test('full PNG capture flow: message → prepare → CDP → download', async () => {
    expect(true).toBe(true);
  });
  test('error handling: restricted URL shows error', async () => {
    expect(true).toBe(true);
  });
  test('error handling: debugger attach failure recovers', async () => {
    expect(true).toBe(true);
  });
});
